NUnit testovi koriste tool ExportSqlCe.exe, a on zahteva
Microsoft SQL Server Compact 3.5 Service Pack 2 for Windows Desktop (instalirati i za x86 i za x64), i NET Framework 4.




Za Git na Windowsu:

The following command tells Git that an end-of-line CR is not an error.
As a result, those annoying ^M characters no longer appear at the end of lines in
git diff, git show, etc:

git config --global core.whitespace cr-at-eol
